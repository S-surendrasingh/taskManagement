source 'https://rubygems.org'

gem 'rails', '~> 6.1'
gem 'sqlite3', '~> 1.4'

# Authentication
gem 'bcrypt', '~> 3.1.7'
gem 'jwt'

# Admin (ActiveAdmin expects non-API app)
gem 'activeadmin'

# Web server
gem 'puma'

# Speed up boot time
gem 'bootsnap', '>= 1.4.4', require: false

group :development do
  gem 'listen', '~> 3.7'
end